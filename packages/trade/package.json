{
  "name": "@estg/trade",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "selftest": "tsx src/hangar.selftest.ts",
    "test": "npm run typecheck && npm run selftest"
  },
  "dependencies": {
    "@estg/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "tsx": "^4.23.13"
  }
}
