# DEPRECATED — replaced by deploy-modules-preview.yml
#
# Trade-only Pages deploy is superseded by the unified modules preview
# (explore + sort + trade in one artifact).
#
# Canonical workflow copy: docs/ci/deploy-modules-preview.yml
# Install path:           .github/workflows/deploy-modules-preview.yml
#
# If this file still exists under .github/workflows/, delete it so only
# Deploy Modules Preview publishes to the github-pages environment.
