# Modules preview (explore + sort + trade) — build always; deploy when Pages opted in.
#
# Single GitHub Pages artifact with subpaths:
#   /           → trade hangar
#   /explore/   → explore
#   /sort/      → sort
#
# Live URLs (owner Xyngwie, project Pages):
#   https://xyngwie.github.io/-explore-sort-trade-game/
#   https://xyngwie.github.io/-explore-sort-trade-game/explore/
#   https://xyngwie.github.io/-explore-sort-trade-game/sort/
#
# To enable Deploy:
#   1. Repo public (or plan with private Pages)
#   2. Settings → Pages → Source: GitHub Actions
#   3. Actions variable ENABLE_GITHUB_PAGES=true
#
# Replaces deploy-trade-preview.yml (trade-only). See docs/PREVIEW_HOSTING_V0.md.

name: Deploy Modules Preview

on:
  push:
    branches: [main]
    paths:
      - "packages/explore/**"
      - "packages/sort/**"
      - "packages/trade/**"
      - "packages/shared/**"
      - ".github/workflows/deploy-modules-preview.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: modules-preview-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Build trade (Pages root)
        env:
          VITE_BASE: ${{ vars.TRADE_VITE_BASE || '/-explore-sort-trade-game/' }}
        run: npm run build:trade

      - name: Build explore (Pages /explore/)
        env:
          VITE_BASE: ${{ vars.EXPLORE_VITE_BASE || '/-explore-sort-trade-game/explore/' }}
        run: npm run build:explore

      - name: Build sort (Pages /sort/)
        env:
          VITE_BASE: ${{ vars.SORT_VITE_BASE || '/-explore-sort-trade-game/sort/' }}
        run: npm run build:sort

      - name: Assemble Pages site
        run: |
          set -euo pipefail
          mkdir -p _site/explore _site/sort
          cp -a packages/trade/dist/. _site/
          cp -a packages/explore/dist/. _site/explore/
          cp -a packages/sort/dist/. _site/sort/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    if: ${{ vars.ENABLE_GITHUB_PAGES == 'true' }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4# Modules preview (explore + sort + trade) — build always; deploy when Pages opted in.
#
# Single GitHub Pages artifact with subpaths:
#   /           → trade hangar
#   /explore/   → explore
#   /sort/      → sort
#
# Live URLs (owner Xyngwie, project Pages):
#   https://xyngwie.github.io/-explore-sort-trade-game/
#   https://xyngwie.github.io/-explore-sort-trade-game/explore/
#   https://xyngwie.github.io/-explore-sort-trade-game/sort/
#
# To enable Deploy:
#   1. Repo public (or plan with private Pages)
#   2. Settings → Pages → Source: GitHub Actions
#   3. Actions variable ENABLE_GITHUB_PAGES=true
#
# Replaces deploy-trade-preview.yml (trade-only). See docs/PREVIEW_HOSTING_V0.md.

name: Deploy Modules Preview

on:
  push:
    branches: [main]
    paths:
      - "packages/explore/**"
      - "packages/sort/**"
      - "packages/trade/**"
      - "packages/shared/**"
      - ".github/workflows/deploy-modules-preview.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: modules-preview-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Build trade (Pages root)
        env:
          VITE_BASE: ${{ vars.TRADE_VITE_BASE || '/-explore-sort-trade-game/' }}
        run: npm run build:trade

      - name: Build explore (Pages /explore/)
        env:
          VITE_BASE: ${{ vars.EXPLORE_VITE_BASE || '/-explore-sort-trade-game/explore/' }}
        run: npm run build:explore

      - name: Build sort (Pages /sort/)
        env:
          VITE_BASE: ${{ vars.SORT_VITE_BASE || '/-explore-sort-trade-game/sort/' }}
        run: npm run build:sort

      - name: Assemble Pages site
        run: |
          set -euo pipefail
          mkdir -p _site/explore _site/sort
          cp -a packages/trade/dist/. _site/
          cp -a packages/explore/dist/. _site/explore/
          cp -a packages/sort/dist/. _site/sort/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    if: ${{ vars.ENABLE_GITHUB_PAGES == 'true' }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
